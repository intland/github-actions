name: "test-Action"
description: "Start test jobs and report back test"
author: "Miklos Gergely"
inputs:
  access_token:
    description: "GitHub token"
    required: false
outputs:
  extra_parameters:
    description: "extra parameters"
runs:
  using: "docker"
  image: "../test.Dockerfile"
